import React from 'react'
import styled from 'styled-components'

const Score = styled.span((props) => (`
    color: #494949;
    font-size: 14px;
    fload: ${ props.float };
`))

const ScoreBar = (props) => (
    <div>
        
    </div>
)
export default TitleBar